agora eu quero uma classe de nome PdTermXmodem para enviar um arquivo. Quero tudo isso em um unico metodo chamadoi sendFileXmodem, e de forma serial como estou listando abaixo, como se fosse projeto procedural
1 - Abre um dialogo para poder escolher o arquivo
2 - Com o arquivo escolhido põe todo seu conteudo em um buffer
3 - Envia o caractere ascii '3' pela serial com isso a placa que vai receber o arquivo entra em modo de recepção Xmodem e envia um NACK
4 - Le porta serial e verifica se recebeu NACK
    se não recebeu depois do timeout dá mensagem de erro e termina
5 - Recebeu NACK log o recebimento
6 - Enviar dados do arquivo pela serial
    Le 128 bytes do buffer, se for ultimo pacote e menor que 128 completa com 0xFF
    se não tem mais para enviar sinaliza no log e termina com sucesso de transmissao
    SOH
    packet_number
    packet_number complemento
    envia os 128 bytes
    envia checksum
    aguarda ACK com timeout
    Se não receber ACK termina com mensagem de erro no log e termina
7 - Recebeu ACK mostra no log que envio do pacote foi sucesso
8 - Vai para o item 6 até terminar
9 - Formato Mensagens no log
    [ERRO_GRAVE] - <msg>
    [ERRO] - <msg>
    [WARN] - <msg>
    [INFO] - <msg>



Eu quero que voce DE FORMA ALGUMA ALTERE UM METODO OU CLASSE QUE NÃO ESTEJA ENVOLVIDA NA NOVA IMPLEMENTAÇÃO.
Nova implementação:
1 - Entrada de dados da serial
    1.1 - Criar um buffer para entrada de dados da serial, com o nome serial_input_buffer.
    1.2 - Criar uma thread de nome serial_read() que fica lendo a serial e ponha os dados que chegaram no serial_input_buffer.
    1.3 - Criar uma flag que autoriza a ler o buffer e escrever no terminal: flag_terminal_write: bool
    1.4 - Criar uma thread de nome write_terminal() que se flag_terminal_write == True fica lendo o serial_input_buffer e escreva no terminal.
          observação: a flag na verdade deve suspender o funcionamento da thread write_terminal() quando ela estiver em false, não é para terminar
          a thread, simplesmente ela não faz nada só verifica a flag e dorme x tempo quando a flag estiver em false.
    1.4 - Criar um metodo de nome read() para ler o buffer serial_input_buffer quando a flag flag_terminal_write == False e ele deve fazer um log
          de ERRO_GRAVE caso ele seja chamado e a flag_termina_write esteja == True, porque isso não pode acontecer.

2 - Saida de dados da serial
    2.1 - Criar um buffer para saida de dados da serial, com o nome serial_output_buffer.
    2.2 - Criar uma thread de nome serial_write() que fica lendo o serial_output_buffer e escrevendo na serial.
    2.3 - Criar uma flag de nome flag_terminal_user_enable: bool
    2.3 - Criar um metodo de nome write() que recebe um dado e escreve no buffer serial_output_buffer, esse metodo deve ter um parametro
          que indica quem está querendo escrever no buffer, inicialmente teremos 2 entidades distintas que farão escritas nesse buffer
          2.3.1 - Terminal pode escrever quando a flag_terminal_user_enable == True
          2.3.2 - Xmodem pode escrever quando a flag_terminal_user_enable == False

Observação: Não deve haver concorrência para escrever no buffer, não há concorrencia para ler ou escrever na serial as flags devem determinar quem
            tem o uso exclusivo de ler ou escrever na serial

3 - Formato Mensagens no log
    [ERRO_GRAVE] - <msg>
    [ERRO] - <msg>
    [WARN] - <msg>
    [INFO] - <msg>
